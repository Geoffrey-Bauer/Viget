{% extends 'base.html.twig' %}

{% block title %}Compte Banni{% endblock %}

{% block body %}
    <div class="flex items-center justify-center min-h-screen bg-gradient-to-br from-red-500 to-red-700">
        <div class="bg-white shadow-lg rounded-lg max-w-md w-full p-8 transform transition-all duration-300 hover:scale-105">
            <div class="flex justify-center mb-6">
                <svg class="w-20 h-20 text-red-600 animate-bounce" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18.364 5.636a9 9 0 10-12.728 12.728M15 12h6m-3-3v6m-3 3h6m-6 0H9m3-3V9"></path>
                </svg>
            </div>
            <h1 class="text-4xl font-extrabold text-red-600 mb-4 text-center">Compte Banni</h1>
            <p class="text-gray-700 mb-6 text-center">
                Votre compte a été banni. Si vous pensez qu'il s'agit d'une erreur, veuillez contacter l'administrateur.
            </p>
            <a href="{{ path('app_logout') }}" class="block w-full text-center bg-red-600 hover:bg-red-700 text-white font-bold py-2 px-4 rounded-lg shadow-lg transition duration-300 ease-in-out transform hover:scale-105 hover:shadow-xl">
                Déconnexion
            </a>
        </div>
    </div>

    <style>
        /* Styles personnalisés pour la page de bannissement */
        .bg-gradient-to-br {
            background: linear-gradient(to bottom right, #f56565, #c53030);
        }

        .animate-bounce {
            animation: bounce 1s infinite; /* Animation de rebond */
        }

        @keyframes bounce {
            0%, 100% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-10px);
            }
        }

        .transform:hover {
            transform: translateY(-2px); /* Légère élévation au survol */
        }

        .hover\:scale-105:hover {
            transform: scale(1.05); /* Agrandissement au survol */
        }

        .transition-all {
            transition: all 0.3s ease; /* Transition douce pour tous les éléments */
        }
    </style>
{% endblock %}